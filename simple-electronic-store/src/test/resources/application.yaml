logging:
  level:
    org:
      springframework:
        r2dbc: TRACE
spring:
  thymeleaf:
    encoding: UTF-8
  r2dbc:
    url: ${CONTAINER.URL:r2dbc:postgresql://localhost:5432/yandex_practicum}
    username: ${CONTAINER.USERNAME:sa}
    password: ${CONTAINER.PASSWORD:}
