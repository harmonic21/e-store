spring:
  jpa:
    show-sql: true
  datasource:
    url: jdbc:h2:mem:web_blog;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALS;INIT=RUNSCRIPT FROM 'classpath:schema.sql'
    driver-class-name: org.h2.Driver
    username: sa
    password: