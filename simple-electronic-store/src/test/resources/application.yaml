logging:
  level:
    org:
      springframework:
        r2dbc: TRACE
spring:
  thymeleaf:
    encoding: UTF-8
  r2dbc:
    url: ${CONTAINER.URL}
    username: ${CONTAINER.USERNAME}
    password: ${CONTAINER.PASSWORD}
